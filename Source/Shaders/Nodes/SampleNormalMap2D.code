FIXED3 SampleNormalMap2D(HLSL(uniform) GLSL(lowp) sampler2D t, HALF2 st GLOBALS) {
	FIXED4 s = tex2D(t, st);
	s.x = s.x * s.w;
	s.xy = (s.xy - FIXED2(0.5, 0.5)) * FIXED2(2.0, 2.0);
	s.z = sqrt(FIXED(1.0) - (s.x*s.x) - (s.y*s.y));
	return s.xyz;
}
